# coverage-ignore.txt
#
# Regex fragments (one per line) for files EXCLUDED from coverage measurement.
#
# Scope: WHOLE PROJECT (per CONTRIBUTING.md "total code coverage"). We exclude only
# NON-PRODUCT code — test code, build intermediates, the toolchain, and SPM checkouts.
# All product source under /Sources/ counts toward the total (including UI).
#
# Consumed by:
#   - scripts/coverage-gate.sh (xccov line filter, as an extended-regex alternation)
#   - llvm-cov export/show -ignore-filename-regex (region coverage + lcov + HTML)
#
# Lines starting with '#' and blank lines are ignored.

# --- test code (not product code) ---
/Tests/

# --- build intermediates / toolchain / dependencies ---
/DerivedData/
/Build/
\.build/
/Applications/Xcode
/usr/
SourcePackages/
